[project]
name = "esp-mcp"
version = "0.1.0"
description = "MCP server for ESP-IDF development - provides tools for building, flashing, monitoring, and debugging ESP32 projects"
readme = "README.md"
requires-python = ">=3.11.0"
dependencies = [
    "mcp[cli]>=1.5.0",
    "pytest>=8.0.0",
]
